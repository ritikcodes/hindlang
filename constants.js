// Storage
const env = {};

// Standard library
const stl = {
  jama: (a, b) => a + b,
  guna: (a, b) => a * b,
  bhag: (a, b) => a / b,
  ghaat: (a, b) => a - b,
};

module.exports = {
  env,
  stl,
};
